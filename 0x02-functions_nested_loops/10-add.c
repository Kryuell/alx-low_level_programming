#include <stdio.h>
#include "main.h"
/**
 * add - Entry point
 *
 * description: the description
 * @a: first param
 * @b: second param
 * Return: Always 0 (Success)
 */
int add(int a, int b)
{
	return (a + b);
}
